{
    "scripts": {
        "clean": "rm -rf dist",
        "build": "npm run clean && tsc",
        "postbuild": "cp secrets/FirebaseServiceAccount.json dist/secrets/FirebaseServiceAccount.json"
    },
    "dependencies": {
        "aws-lambda": "^1.0.7",
        "axios": "^1.10.0",
        "firebase-admin": "^13.4.0",
        "typescript": "^5.8.3"
    },
    "devDependencies": {
        "@types/aws-lambda": "^8.10.150",
        "@types/form-data": "^2.2.1",
        "@types/node": "^24.0.4",
        "undici": "^7.10.0"
    }
}
